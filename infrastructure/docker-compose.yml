# ============================================================================
# ⚠️  DEPRECATED - Use environment-specific files instead
# ============================================================================
#
# This file is kept for backward compatibility but should NOT be used.
#
# Instead, use:
#   - docker-compose.dev.yml  (Development)
#   - docker-compose.uat.yml  (UAT/Staging)
#   - docker-compose.prod.yml (Production template)
#
# Quick start:
#   ./start-dev.sh    # Start development environment
#   ./start-uat.sh    # Start UAT environment
#
# Or manually:
#   docker-compose -f docker-compose.dev.yml --env-file .env.dev up -d
#
# ============================================================================

version: '3.8'

# This is a symbolic link to the development environment
# If you run "docker-compose up", it will use dev environment

include:
  - path: docker-compose.dev.yml
