# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=taxdividend_dev
DB_USERNAME=taxdividend_user
DB_PASSWORD=changeme

# MinIO / S3 Storage Configuration
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=changeme
MINIO_SECRET_KEY=changeme
MINIO_BUCKET=tax-dividend-forms

# Security Configuration
INTERNAL_API_KEY=changeme-internal-api-key-min-32-chars
JWT_SECRET_KEY=changeme-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256

# Email / SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=changeme@example.com
SMTP_PASSWORD=changeme
SMTP_FROM=noreply@taxdividend.com

# Actuator Security
ACTUATOR_USERNAME=admin
ACTUATOR_PASSWORD=changeme-strong-password

# Redis Configuration (optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# OpenTelemetry / Tracing
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# Application Configuration
APP_FRONTEND_URL=http://localhost:5173

# Keycloak Configuration
KEYCLOAK_SERVER_URL=http://localhost:8180
KEYCLOAK_REALM=tax-dividend
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=changeme
KEYCLOAK_CLIENT_ID=backend-service
